camunda.bpm:
  admin-user:
   id: demo
   password: demo
   firstName: Demo
  filter:
   create: All tasks
server:
  port: 8081

spring:
  cloud:
    stream:
      kafka:
        bindings:
          takeFromBankAccountQueue-in-0:
            destination: saveBankAccountTopic
        binder:
          brokers: localhost:9092
